import { View } from 'react-native';
import { Text } from 'src/components/ui';

export default function ProfileScreen() {
    return (
        <View className="flex-1 justify-center items-center">
            <Text className="text-primary text-4xl">Profile</Text>
        </View>
    );
}
